# Python backend dependencies — pinned to exact versions for reproducibility.
# To update: run `pip freeze` in the venv and update versions here.

Flask==3.1.3
flask-cors==6.0.5
flask-socketio==5.5.1
joblib==1.5.3
matplotlib==3.11.1
numpy==2.4.3
pandapower==3.5.4
pandas==2.3.3
scikit-learn==1.8.0
shap==0.52.0
xgboost==3.4.0
paho-mqtt==2.1.0

# Temporal model (Section 4.1) — install separately if not already present:
tensorflow>=2.15.0
